# Final numerical regression

The initial report records225 selected tests. One extra test was then added for artificially large but finite values in the paired-method correlation calculation. Separate square roots prevent overflow of a product of otherwise representable variances. Final selected test count:226 (69 new,157 prior selected),47 standalone paired-method tests. TEST_STATUS_FINAL.json supersedes the earlier test-count snapshot. No scientific scenario, coefficient, profile or mortality result changed. This numerical control is synthetic, not additional clinical evidence.
