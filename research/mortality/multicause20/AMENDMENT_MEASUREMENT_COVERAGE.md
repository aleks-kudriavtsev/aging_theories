# Measurement-coverage amendment, before model performance

2026-09-15. The official files have been retrieved and baseline measurement availability inspected. No model predictions, performance metrics, or comparative results have been calculated.

Observed source structure agrees with the codebook control counts: serum creatinine/BUN are present only in the augmentation sample (subsample3), whereas serum albumin is absent there. The original locations1-65 therefore contain zero joint albumin+creatinine/BUN measurements. These cannot be silently imputed or merged across persons. Primary comparison remains unchanged: age, sex-specific baseline, BMI, SBP, with/without measured albumin, total cholesterol and hemoglobin.

The conditional secondary detailed-sample comparison is consequently specified as clinical+baseline smoking versus the same clinical inputs plus core laboratory values and AST, alkaline phosphatase and uric acid (source-native legacy chemistry units). It is restricted to original detailed sample2 with SWDP2 weights and the same heldout-strata assignment as the primary study. Renal creatinine/BUN indicators remain catalogued but are not fitted in this joint panel. This is a pre-performance availability amendment, not a favourable-result selection. Both study domains, all comparisons and exclusions are reported separately.

Technical mathematical specification: for each panel an all-cause piecewise-exponential model supplies the coefficient shrinkage target, refitted within each training fold. Cause-specific slopes have ridge penalty lambda from1,10,100 toward that target. Sex-by-interval log baselines have weak ridge0.25 toward a same-training empirical anchor; the anchor uses0.5 continuity smoothing solely to avoid log(0), not additional observed events. Source event counts are never increased. Slopes are bounded[-5,5], log baselines[-25,5] for numerical stability; boundaries and convergence are audited. Prostate intensity is exactly zero for recorded female sex; its model uses male training rows. Unselected causes are pooled into the mixed residual, not declared absent. All eligible observed categories use the same target partition fixed from outer DEVELOPMENT events only.

The exact source code and hashes will accompany the results. Model-feature selection and reported laboratory modules now reflect measurements genuinely available in the same individuals.
